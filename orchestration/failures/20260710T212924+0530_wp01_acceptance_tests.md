# Failure report

## Task

T-WP01 repository foundation acceptance tests.

## Failed command

```text
python3 -m pytest
```

Exit code: `1`.

## Error

One test failed:

```text
tests/unit/test_config.py::test_cli_help_and_validate_config
SystemExit: 0
```

The test called `main(["--help"])` and expected an integer return value. Python `argparse` handles the standard `--help` action by printing help and raising `SystemExit(0)` before `main` can return.

Test summary: 5 passed, 1 failed, 6 collected.

## Files changed before failure

The T-WP01 foundation files were created before the test run:

- `pyproject.toml`
- `Makefile`
- `README.md`
- `src/semifab_poc/__init__.py`
- `src/semifab_poc/config.py`
- `src/semifab_poc/logging.py`
- `src/semifab_poc/cli.py`
- `tests/unit/test_config.py`
- `docs/README.md`
- `docs/architecture.md`

No files were changed after the failing test. Task status was not advanced.

## Read-only diagnosis

- The failure is deterministic and isolated to the test's direct invocation of the CLI entry point.
- The CLI's behavior matches standard `argparse` semantics for `--help` and the process-level command `python3 -m semifab_poc.cli --help` is expected to exit successfully.
- The test's assumption that `main(["--help"])` returns `0` is incompatible with the current parser implementation.
- The remaining five configuration and CLI-validation tests passed.
- No scientific model, dataset, network action, optional dependency, or external service was involved.

## Likely causes

The test was authored as if `main` were a pure return-code function, but `argparse`'s built-in help action is process-oriented and raises `SystemExit` by design.

## Recovery options

1. Adjust the unit test to assert `pytest.raises(SystemExit)` with code `0` for direct help invocation, and separately smoke-test the process CLI.
2. Introduce a custom non-exiting parser or catch `SystemExit` inside `main`; this changes standard CLI semantics and is unnecessary for the package contract.
3. Remove help coverage; this would weaken the explicit `CLI_help_succeeds` acceptance test.

## Recommended option

Option 1. Preserve standard `argparse` behavior, update the unit test expectation, and add a process-level help smoke test. This is a bounded test correction, not a scientific or architectural change.

## User decision required

Confirm that I may apply Option 1, rerun the T-WP01 acceptance suite, and continue only if it passes.
