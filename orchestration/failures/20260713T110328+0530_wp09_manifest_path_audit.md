# Failure report

## Task

WP09 pre-implementation audit of the processed PHM 2016 CMP bundle and its
provenance manifest.

## Failed command

```text
sed -n '1,260p' orchestration/decisions/wp12_early_warning_target.md; sed -n '1,260p' configs/models/early_warning.yaml; sed -n '1,320p' src/semifab_poc/data/splits.py; sed -n '1,260p' configs/data/phm_2016_cmp.yaml; sed -n '1,260p' data/processed/phm_2016_cmp/manifest.json
```

## Error

```text
sed: can't read data/processed/phm_2016_cmp/manifest.json: No such file or directory
```

## Files changed before failure

None. The failed operation was read-only. This failure report is the only file
created after the failure.

## Read-only diagnosis

`rg --files data/processed/phm_2016_cmp` and a manifest-name search show that
the processed bundle contains
`data/processed/phm_2016_cmp/feature_manifest.yaml`. There is no
`manifest.json` at that location. The command used an incorrect filename and
extension; it did not reveal a missing processed-data bundle.

## Likely causes

The audit command relied on an inaccurate remembered path instead of the
repository's actual `feature_manifest.yaml` name.

## Recovery options

1. Read the existing `feature_manifest.yaml`, verify all referenced processed
   artifacts and hashes, and resume the frozen-design work without changing
   data.
2. Stop WP09 and perform a broader read-only inventory of all data provenance
   files before resuming.
3. Leave WP09 paused.

## Recommended option

Option 1. The correct provenance file exists, the processed bundle is present,
and no mutation or scientific result occurred before the failure.

## User decision required

Authorize Option 1 before any further WP09 mutation or execution.
