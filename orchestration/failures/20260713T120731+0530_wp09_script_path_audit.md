# Failure report

## Task

Read-only inspection of repository script conventions before adding the guarded WP09 runner.

## Failed command

```text
sed -n '1,260p' scripts/build_wp10_demo.py
```

This command was the final segment of a larger read-only inspection command.

## Error

```text
sed: can't read scripts/build_wp10_demo.py: No such file or directory
```

## Files changed before failure

No files were changed during this turn before the failure. The following previously authorized WP09 work remains uncommitted and was preserved unchanged:

- `orchestration/project_status.md`
- `orchestration/task_manifest.yaml`
- `orchestration/failures/20260713T115920+0530_wp09_experiment_module_path_audit.md`
- `src/semifab_poc/models/virtual_metrology.py`
- `tests/integration/test_vm_evaluation.py`
- `tests/unit/test_virtual_metrology.py`

This failure report is the only mutation made after the failure.

## Read-only diagnosis

`rg --files scripts` confirms that `scripts/build_wp10_demo.py` does not exist. The available WP10 entry point is `scripts/validate_wp10_coupling.py`. The nonexistent path was introduced by assuming a demonstration-script name instead of selecting a path from the immediately preceding `rg --files` output.

The WP09 syntax and formatting preflight completed before this inspection and was clean: the module parsed successfully, no trailing whitespace was reported, and `git diff --check` returned no findings. No official test or validation MRR target was opened.

## Likely causes

1. A stale inferred filename was included in the inspection command.
2. The command was assembled with multiple paths before validating every requested path against the discovered file list.

## Recovery options

1. Resume using only paths copied from the verified `rg --files scripts` output, inspect `scripts/validate_wp10_coupling.py` and `scripts/validate_wp12_early_warning.py`, then continue the guarded WP09 script and synthetic-only tests.
2. Pause WP09 and audit every remaining planned path before any further implementation.

## Recommended option

Option 1. It directly addresses the path error, preserves the validated WP09 work, and does not require opening holdout targets.

## User decision required

Authorize recovery option 1 before implementation resumes.
