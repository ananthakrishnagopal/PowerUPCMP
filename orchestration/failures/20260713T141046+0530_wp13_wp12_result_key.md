# Failure report

## Task

Perform the initial read-only WP13 root-cause-attribution inventory and inspect
the frozen WP12 result structure for reusable warning evidence.

## Failed command

A read-only Python command loaded
`reports/early_warning/wp12_validation.json` and attempted to access
`d["models"]`.

## Error

`KeyError: 'models'` with exit code 1.

## Files changed before failure

None. The WP09 checkpoint remained clean at commit `a1a8999`, and this WP13
turn had performed read-only inspection only.

## Read-only diagnosis

The frozen WP12 result uses the top-level key `model_results`, not `models`.
Its model keys are `PREVALENCE`, `LOGISTIC`, and `GRADIENT_BOOSTED`. The command
assumed an unverified JSON shape even though the top-level keys were available
in the same output.

## Likely causes

1. The ad hoc inspection command inferred a conventional key name instead of
   inspecting the artifact schema first.
2. The WP12 report structure predates the current WP13 inventory and has no
   separate typed reader for exploratory reporting code.

## Recovery options

1. Recommended: continue the read-only WP13 inventory using the verified
   `model_results` key and inspect nested keys incrementally before freezing the
   attribution protocol. Do not modify WP12 artifacts.
2. Avoid reusing any WP12 result metadata and design WP13 only from source code
   and configuration, losing useful warning-time context.
3. Add a typed WP12 result reader before WP13 design, expanding scope without a
   present implementation need.

## Recommended option

Option 1. It preserves frozen WP12 evidence and supports the intended WP13
design review with the smallest read-only recovery.

## User decision required

Authorize option 1 before continuing the WP13 inventory or making any project
change.
